# Equivalent Mutant Analysis

Aggregates classifier predictions into thesis tables, statistics, and plots.

Configuration lives in `config.yaml`. Outputs go to `output/` and logs to `logs/`.

See `../README.md` for full pipeline usage.
